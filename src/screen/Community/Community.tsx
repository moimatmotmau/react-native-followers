import React from "react";
import styled from "styled-components/native";

type Props = {};

export const Community = (props: Props) => {
  return <Style.View></Style.View>;
};

const Style = {
  View: styled.View`
    flex: 1;
  `,
};
